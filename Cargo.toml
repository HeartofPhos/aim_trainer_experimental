[workspace]
resolver = "3"
members = ["crates/aim_trainer", "crates/ref_asset", "crates/schema"]

[workspace.dependencies]
glam = { version = "0.33", features = ["serde"] }
glamx = { version = "0.3", features = ["serde"] }
serde = { version = "^1.0.220", features = ["derive"] }
serde_json = { version = "1.0.145", features = [
    "arbitrary_precision",
    "preserve_order",
] }
rand = "0.10"
raylib = { version = "6.0.0", features = ["glam"] }
serde_with = "3.12.0"
thiserror = "2.0"

[patch.crates-io]
raylib = { git = 'https://github.com/HeartofPhos/raylib-rs.git', branch = 'unstable' }

[workspace.lints]

# Enable a small amount of optimization in the dev profile.
[profile.dev]
opt-level = 1

# Enable a large amount of optimization in the dev profile for dependencies.
[profile.dev.package."*"]
opt-level = 3

[profile.release]
# Compile the entire crate as one unit.
# Slows compile times, marginal improvements.
codegen-units = 1
# Do a second optimization pass over the entire program, including dependencies.
# Slows compile times, marginal improvements.
lto = "thin"
